apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: prod-fixfun
  namespace: production-fixfun
spec:
  refreshInterval: "10m"
  secretStoreRef:
    name: custom-store
    kind: ClusterSecretStore
  target:
    name: prod-fixfun
  data:
    - secretKey: prod-fixfun
      remoteRef:
        key: prod-fixfun
